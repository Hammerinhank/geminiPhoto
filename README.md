Se lance avec
cd /Users/francoistavernier/Desktop/python/python_gemini_photo && python3 app.py

Voici une solution complète sous forme de deux petits fichiers à placer dans le même dossier sur votre iMac : 

un fichier app.py (le script Python qui se détache du terminal pour ouvrir une fenêtre propre) 

et un fichier index.html (l'interface web qui gère le stockage local, le copier-coller/glisser-déposer d'images et l'appel direct à l'API Gemini).

Se lance avec
cd /Users/francoistavernier/Desktop/python/python_gemini_photo && python3 app.py